import { ReactNode } from 'react';
import './PageMenuContent.css';
export interface PageMenuContentProperties {
    children: ReactNode;
}
export declare const PageMenuContent: ({ children, }: PageMenuContentProperties) => JSX.Element;
